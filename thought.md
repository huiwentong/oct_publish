# 关于新的publish工具的构想设计
<details>
<summary>核心摘要</summary>

> 介绍： 
>> 关于为什么要重新设计publish工具，主要是原有publish工具框架十分老旧，且设计思路已经逐渐不再符合日益变化的技术需求
>> 
>> 且后续如果ai会接入到工作流程中面对现有纯ui操作式的publish工具会无从下手
>>
>> 另外就是外包如果要回收文件或者未来有批量发布的需求时，往往都需要手动重新写检查发布逻辑或者开发脚本来对ui式的publish工具做模拟人为操作，这样会造成不少的人力浪费，反复做重复的开发工作
>>
>> 且如果我们主要的pulish流程中发生任何更新，那么这些临时批量publish脚本、外包回收pulish脚本都要同步做修改，后续维护起来十分繁琐。
>> 
>> 且老的publish工具的log方式和发布的设计都和现代化的技术框架稍微脱节，会造成大家在发布版本时候的体验感不太好。
>>
> 计划:
>> 先由我来设计敲定新的publish框架，后续会快速完成一个demo，来给大家做演示
>>
>> 再根据大家反馈来最publish的框架做补充修改
>>
>> 最后会给大家根据大家负责的环节来安排组件的开发任务，由大家评估开发周期、时间
>> 
>> 由于publish是十月流程框架中的核心部分，每个环节都会涉及到publish的组件开发，在大家升级组件的时候可以进行分步升级，优先升级一些相对小简单的环节，试行一段时间没问题后再根据大家的开发排期，逐步进行完善，在升级期间新老publish工具是可以同时使用的，不会耽误项目正常的推进。

</details>

---

<details>
<summary>设计需求</summary>

***关于publish工具的设计需求***

- 新的publish工具要能够支持ui操作，也可以支持cli操作，两种方式本质上调用的是相同的核心逻辑，为的是将来能够不仅人为的进行发布操作，当有大量低优先级的资产或者外包回收资产也需要发布时可以使用cli进行批量快速的发布。

- 新的publish工具要使用数据库来存放log，方便快速定位查找以及回溯汇总，同时这些log也可以通过命令调出来在ui中显示。

- 新的publish工具要能够尽可能做好注释，方便未来可能ai agent来帮忙进行发布操作以及抓取问题并自动快速的进行最终分析。

- 新的publish工具要拆分为三个互不耦合的独立部分：ui部分、核心逻辑部分、可自由组装的发布组件部分

- ui端的publish在启动时可以运行一个后台脚本服务，方便远程对目标机器运行一些DCC的命令来进行快速纠错，对于一些小问题无需到艺术家工位上进行查看。

</details>

---

## ui部分

- ui部分是使用QtPy来做各个不同的qt框架和版本的兼容，界面结构清晰简单，一目了然。

- ui中会加入一个服务组件，ui启动后服务会跟着一起启动直到程序退出，这个服务会等待外部发送一些python的命令或者cmd的命令来执行，并将执行结果反馈给发送命令的客户端


## 核心逻辑部分

- 核心逻辑会提供各个必要的参数作为入口，并提供一些发布状态缓存功能，发布结束清理这些缓存状态，失败重启后会检查这些缓存状态方便断点续传。

- 需要提供快速debug组件的功能，在启动核心后的环境中能够做到快速调用单个组件执行并检查执行结果和stdout的内容。

- 核心的log要体现在三块，一块是命令行执行时会将log输出到控制台，另一块会看是否有ui接入，有的话会将log也输出给ui的接口中，最后一块会将log录入到log的数据库中，会记录使用人，log时间，log等级，log内容


## 组件部分

- 组件要针对不同项目不同环节的检查项和发布项进行隔离设计，但是不再使用原有的特别繁琐的路径，对组件的位置进行重新设计

